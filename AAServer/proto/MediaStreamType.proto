// Distributed under GPLv3 only as specified in repository's root LICENSE file

syntax="proto2";

package tag.aas;

message MediaStreamType
{
    enum Enum
    {
        None = 0;
        Audio = 1;
        Video = 3;
    }
}
